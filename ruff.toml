# Линтер и форматтер (Ruff). Лёгкий набор правил; E/F/I — базовые.
lint.select = ["E", "F", "I", "W", "UP"]
lint.ignore = ["E501", "UP035"]  # длинные строки и устаревшие имена типов — осознанно
target-version = "py311"
line-length = 120
